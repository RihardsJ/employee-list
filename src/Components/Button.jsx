import styled from "styled-components";

const Button = styled.button`
  cursor: pointer;
  font-size: 1em;
  height: 2rem;
  padding: 0.25em 1em;
  border-radius: 15px;
  border-style: none;
`;

export default Button;
